import QtQuick 2.12

Item {
    id: pageRepeatingFraction

    property string athName: "Repeating fraction"
    property int requestedWidth: width
    property int requestedHeight: 500

    //From repeating fraction to rational?? is it needed

    //Rythm and polyrythm baed on binnary numbers at least animation
    //Another poly example is circle with different amount of point

    //6 period classification
}
